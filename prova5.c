#include <stdio.h>

int main() 
{
  int num=-88;

  if (num> 0)
            if (num > 50) printf ("ANA");
            else printf("BIA");
  else
            if (num < 0) printf ("caio");
            else printf("duda");
  

  return 0;
}